# Marathon-Light — Runner magenta window border on cream paper
$activeBorderColor = rgb(EA027E)

general {
    col.active_border = $activeBorderColor
}

group {
    col.border_active = $activeBorderColor
}

# Cream backdrop tolerates higher opacity for readability
windowrule = opacity 0.98 0.95, match:tag terminal
